inputs: {}
resources:
  web1:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - nginx
      count: 1
      image: ubuntu
      constraints:
        - tag: env:aws
      flavor: small
  web2:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - postgresql
      count: 1
      image: ubuntu
      constraints:
        - tag: env:aws
      flavor: small
  web3:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - postgresql
      count: 1
      image: ubuntu
      constraints:
        - tag: env:aws
      flavor: small
