inputs: {}
resources:
  web1:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - apache2
      count: 1
      image: ubuntu
      constraints:
        - tag: cloud:aws
      flavor: small
