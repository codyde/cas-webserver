inputs: {}
resources:
  web1:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - nginx
          - postgresql
      count: 1
      image: ubuntu
      constraints:
        - tag: env:vsphere
      flavor: small
  web2:
    type: Cloud.Machine
    properties:
      cloudConfig: |
        #cloud-config
        packages:
          - nginx
          - postgresql
      count: 1
      image: ubuntu
      constraints:
        - tag: env:vsphere
      flavor: small
